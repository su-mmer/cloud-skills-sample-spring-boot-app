name: Java CI/CD with Self-hosted runner in Action

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        
    - name: Build with Gradle
      uses: gradle/gradle-build-action@bd5760595778326ba7f1441bcf7e88b49de61a25 # v2.6.0
      with:
        arguments: clean build

    - name: Deploy Prod use SCP
      uses: appleboy/scp-action@master
      with:
        username: ubuntu
        host: self-hosted
        key: ${{ secrets.PRIVATE_KEY }}
        source: "./"
        target: "/home/ubuntu/deploy"

  # deploy:
  #   needs: build  # build job 종료 후 deploy job 수행
  #   runs-on: self-hosted
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v3

  #     - name: Set up JDK 11
  #       uses: actions/setup-java@v3
  #       with:
  #         java-version: '11'
  #         distribution: 'temurin'